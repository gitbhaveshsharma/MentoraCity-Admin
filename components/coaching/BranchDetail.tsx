"use client";
import Link from "next/link";
import { useState } from "react";
import { CoachingBranch, CoachingCenter } from "@/lib/types";
import { SeoSheet } from "@/components/seo/SeoSheet";
import { SeoHealthScore } from "@/components/seo/SeoHealthScore";
import { GscSeoOverview } from "@/components/seo/GscSeoOverview";

export function BranchDetail({ branch, center }: { branch: CoachingBranch; center: CoachingCenter }) { const [tab, setTab] = useState<"overview" | "seo">("overview"); const [open, setOpen] = useState(false); const [seo, setSeo] = useState(branch.metadata.seo); const address = branch.address; return <main className="content"><div className="crumb" style={{ marginBottom: 14 }}><Link href={`/coachings/${center.id}`} className="crumb-link">{center.name}</Link> <span> / Branch / </span> {branch.name}</div><div className="page-head"><div><h1 className="page-title">{branch.name}</h1><p className="page-sub">Branch details and SEO controls.</p></div><button className="btn btn-primary" onClick={() => setOpen(true)}>Edit SEO</button></div><div className="detail-tabs"><button className={`pill ${tab === "overview" ? "active" : ""}`} onClick={() => setTab("overview")}>Overview</button><button className={`pill ${tab === "seo" ? "active" : ""}`} onClick={() => setTab("seo")}>SEO editor <SeoHealthScore seo={seo} compact /></button></div>{tab === "overview" ? <div className="detail-grid"><section className="card"><h2 className="card-title">Branch profile <span className="badge badge-muted" style={{ float: "right" }}>🔒 Locked</span></h2><p className="meta-body">{branch.description || "No branch description provided."}</p><div className="info-row"><span>Manager</span><b>{branch.manager?.full_name || branch.manager?.email || "Not assigned"}</b></div><div className="info-row"><span>Phone</span><b>{branch.phone || "Not provided"}</b></div><div className="info-row"><span>Email</span><b>{branch.email || "Not provided"}</b></div><div className="info-row"><span>Teaching mode</span><b>{branch.teaching_mode || "—"}</b></div></section><section className="card"><h2 className="card-title">Branch address</h2><div className="map-placeholder">{address?.latitude && address.longitude ? `Google Maps preview · ${address.latitude}, ${address.longitude}` : "No map coordinates provided"}</div><div className="info-row"><span>Address</span><b>{address?.postal_address || [address?.address_line_1, address?.address_line_2, address?.city, address?.state, address?.pin_code].filter(Boolean).join(", ") || "No address provided"}</b></div><div className="info-row"><span>Place ID</span><b style={{ fontSize: 10 }}>{address?.google_place_id || "—"}</b></div></section></div> : <section className="card" style={{ marginTop: 23 }}><h2 className="card-title">Search appearance</h2><div className="info-row"><span>Title source</span><b className="badge badge-source">{seo.title.source}</b></div><div className="info-row"><span>Description source</span><b className="badge badge-source">{seo.description.source}</b></div><SeoHealthScore seo={seo} /><button className="btn btn-primary" style={{ marginTop: 17 }} onClick={() => setOpen(true)}>Open full SEO editor</button></section>}<GscSeoOverview entityType="branch" entityId={branch.id} /><SeoSheet open={open} onClose={() => setOpen(false)} seo={seo} entityName={branch.name} entityId={branch.id} entityType="branch" onSaved={setSeo} /></main>; }
