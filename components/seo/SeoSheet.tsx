"use client";
import { useEffect, useState } from "react";
import { useForm } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import { toast } from "sonner";
import { SeoPayload } from "@/lib/types";
import { seoSchema, type SeoFormValues } from "@/lib/validations/seo.schema";
import { OgImageUploader } from "@/components/seo/OgImageUploader";

function Counter({ value, min, max }: { value?: string; min: number; max: number }) { const count = value?.length ?? 0; return <span className={`counter ${count < min || count > max ? "invalid" : ""}`}>{count} / {max}</span>; }

export function SeoSheet({ open, onClose, seo, entityName, entityId, entityType = "branch", onSaved }: { open: boolean; onClose: () => void; seo: SeoPayload; entityName: string; entityId: string; entityType?: "center" | "branch"; onSaved?: (seo: SeoPayload) => void }) {
  const [saving, setSaving] = useState(false); const [schema, setSchema] = useState(seo.schema);
  const { register, watch, setValue, reset, handleSubmit, formState: { errors } } = useForm<SeoFormValues>({ resolver: zodResolver(seoSchema), defaultValues: { titleSource: seo.title.source, titleCustom: seo.title.custom, generatedTitle: seo.title.generated, descriptionSource: seo.description.source, descriptionCustom: seo.description.custom, generatedDescription: seo.description.generated, canonicalUrl: seo.canonical_url, index: seo.robots.index, follow: seo.robots.follow, ogTitle: seo.og.title, ogDescription: seo.og.description, twitterTitle: seo.twitter.title, twitterDescription: seo.twitter.description, ogImage: seo.og.image } });
  const titleSource = watch("titleSource"); const descriptionSource = watch("descriptionSource"); const index = watch("index");
  useEffect(() => { reset({ titleSource: seo.title.source, titleCustom: seo.title.custom, generatedTitle: seo.title.generated, descriptionSource: seo.description.source, descriptionCustom: seo.description.custom, generatedDescription: seo.description.generated, canonicalUrl: seo.canonical_url, index: seo.robots.index, follow: seo.robots.follow, ogTitle: seo.og.title, ogDescription: seo.og.description, twitterTitle: seo.twitter.title, twitterDescription: seo.twitter.description, ogImage: seo.og.image }); setSchema(seo.schema); }, [seo, reset]);
  if (!open) return null;
  const submit = async (values: SeoFormValues) => { setSaving(true); const next: SeoPayload = { ...seo, title: { source: values.titleSource, custom: values.titleSource === "custom" ? values.titleCustom : null, generated: values.generatedTitle }, description: { source: values.descriptionSource, custom: values.descriptionSource === "custom" ? values.descriptionCustom : null, generated: values.generatedDescription }, canonical_url: values.canonicalUrl, robots: { index: values.index, follow: values.follow }, og: { ...seo.og, title: values.ogTitle, description: values.ogDescription, image: values.ogImage, url: values.canonicalUrl }, twitter: { ...seo.twitter, title: values.twitterTitle, description: values.twitterDescription, image: values.ogImage }, version: seo.version + 1 }; onSaved?.(next); try { const response = await fetch("/api/seo/update", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ type: entityType, id: entityId, seo: next }) }); if (!response.ok) throw new Error((await response.json().catch(() => null))?.error ?? "Could not save changes"); toast.success("SEO updated", { description: "Custom values will be used on the website." }); } catch (error) { onSaved?.(seo); toast.error("Save failed", { description: error instanceof Error ? error.message : "Could not save changes" }); } finally { setSaving(false); } };
  const regenerate = async () => { try { const response = await fetch("/api/seo/regenerate-schema", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ type: entityType, id: entityId }) }); if (!response.ok) throw new Error("Schema regeneration failed"); const data = await response.json(); setSchema(data.schema ?? schema); toast.info("Schema regenerated", { description: "JSON-LD rebuilt from latest data" }); } catch { const generated = { ...schema, name: entityName, dateModified: new Date().toISOString() }; setSchema(generated); toast.info("Schema regenerated", { description: "Preview rebuilt from current entity data" }); } };
  return <><div className="sheet-backdrop" onClick={onClose} /><section className="sheet" role="dialog" aria-modal="true" aria-label="Edit SEO"><header className="sheet-head"><div><h2>Edit SEO</h2><p>{entityName} · SEO metadata only</p></div><button className="icon-btn" onClick={onClose} aria-label="Close">×</button></header><form id="seo-form" className="sheet-body" onSubmit={handleSubmit(submit)}><div className="seo-alert">Custom values are always preferred. When a valid custom title or description is provided, the website uses it instead of the generated value.</div><div className="form-section"><h3>A · Title</h3><div className="field"><label>SEO title <Counter value={titleSource === "custom" ? watch("titleCustom") ?? "" : watch("generatedTitle")} min={30} max={70} /></label><div className="pills"><button type="button" className={`pill ${titleSource === "generated" ? "active" : ""}`} onClick={() => setValue("titleSource", "generated")}>Use generated</button><button type="button" className={`pill ${titleSource === "custom" ? "active" : ""}`} onClick={() => setValue("titleSource", "custom")}>Use custom</button></div></div>{titleSource === "generated" ? <div className="field"><input {...register("generatedTitle")} readOnly /></div> : <div className="field"><input {...register("titleCustom")} placeholder="30–70 characters" />{errors.titleCustom && <small className="counter invalid">{errors.titleCustom.message}</small>}</div>}</div><div className="form-section"><h3>B · Meta description</h3><div className="field"><label>Description <Counter value={descriptionSource === "custom" ? watch("descriptionCustom") ?? "" : watch("generatedDescription")} min={50} max={160} /></label><div className="pills"><button type="button" className={`pill ${descriptionSource === "generated" ? "active" : ""}`} onClick={() => setValue("descriptionSource", "generated")}>Use generated</button><button type="button" className={`pill ${descriptionSource === "custom" ? "active" : ""}`} onClick={() => setValue("descriptionSource", "custom")}>Use custom</button></div></div>{descriptionSource === "generated" ? <textarea {...register("generatedDescription")} readOnly /> : <textarea {...register("descriptionCustom")} placeholder="50–160 characters" />}{errors.descriptionCustom && <small className="counter invalid">{errors.descriptionCustom.message}</small>}</div><div className="form-section"><h3>C · Canonical URL</h3><div className="field"><label>Canonical URL <span className="badge badge-muted">Auto-derived</span></label><input {...register("canonicalUrl")} /></div></div><div className="form-section"><h3>D · Robots</h3><div className="info-row"><span>Index this page</span><button type="button" className={`toggle ${index ? "on" : ""}`} onClick={() => setValue("index", !index)} aria-label="Toggle index" /></div><div className="info-row"><span>Follow links</span><button type="button" className={`toggle ${watch("follow") ? "on" : ""}`} onClick={() => setValue("follow", !watch("follow"))} aria-label="Toggle follow" /></div>{!index && <div className="warning">This page will be excluded from search engines.</div>}</div><div className="form-section"><h3>E · Social preview</h3><div className="field"><label>OG title</label><input {...register("ogTitle")} /></div><div className="field"><label>OG description <Counter value={watch("ogDescription")} min={0} max={160} /></label><textarea {...register("ogDescription")} /></div><div className="field"><label>OG image</label><OgImageUploader centerId={entityType === "center" ? entityId : seo.canonical_url} branchId={entityId} value={watch("ogImage")} onChange={(url) => setValue("ogImage", url, { shouldDirty: true })} /><input {...register("ogImage")} placeholder="Or paste an image URL" style={{ marginTop: 8 }} /><small style={{ color: "var(--color-muted-foreground)", fontSize: 11 }}>Recommended: 1200×630px, max 500KB, .webp preferred.</small></div><div className="field"><label>Twitter title</label><input {...register("twitterTitle")} /></div><div className="field"><label>Twitter description <Counter value={watch("twitterDescription")} min={0} max={160} /></label><textarea {...register("twitterDescription")} /></div></div><div className="form-section"><h3>F · Schema.org JSON-LD</h3><pre className="json-view">{JSON.stringify(schema, null, 2)}</pre><button type="button" className="btn btn-ghost btn-sm" style={{ marginTop: 10 }} onClick={regenerate}>↻ Regenerate schema</button></div></form><footer className="sheet-foot"><button className="btn btn-ghost" type="button" onClick={onClose}>Discard changes</button><button className="btn btn-primary" type="submit" form="seo-form" disabled={saving}>{saving ? "Saving…" : "Save SEO"}</button></footer></section></>;
}
